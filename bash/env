# vim: set filetype=sh

# For Android SDK
[ -d "/opt/android-sdk-macosx" ] && export PATH=$PATH:/opt/android-sdk-macosx/tools:/opt/android-sdk-macosx/platform-tools

# For Haskell
[ -d "$HOME/Library/Haskell" ] && export PATH="$PATH:$HOME/Library/Haskell/bin"

# Setup root path of go tools
command -v go >/dev/null 2>&1
if [ $? -eq 0 ]; then
  export GOROOT=`go env GOROOT`
  export GOPATH=$HOME/src/go
  export PATH=$PATH:$GOROOT/bin:$GOPATH/bin
fi

# For personal bin
export PATH=~/bin:$PATH

# Use mvim (nofork) as editor
export EDITOR="mvim -f"
